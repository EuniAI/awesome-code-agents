# Self-hosted star history chart. star-history.com now needs a per-viewer token
# (GitHub restricted stargazer data), so its embedded SVG is permanently broken.
# We render our own SVG from the repo's stargazers and commit it weekly.
name: Star history chart

on:
  schedule:
    - cron: "17 4 * * 1" # Mondays 04:17 UTC
  workflow_dispatch:

permissions:
  contents: write

jobs:
  chart:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - name: Render chart
        env:
          GH_TOKEN: ${{ github.token }}
        run: python assets/generate_star_history.py

      - name: Commit if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets/star-history.svg
          if git diff --cached --quiet; then
            echo "Star history unchanged."
            exit 0
          fi
          git commit -m "chore(assets): refresh star history chart"
          # Only this job writes this file, so a rebase never conflicts on it;
          # the retry just absorbs a lost push race with another workflow.
          for attempt in 1 2 3; do
            git fetch --quiet origin main
            if git rebase --quiet origin/main && git push --quiet; then
              echo "Pushed on attempt $attempt."; exit 0
            fi
            git rebase --abort 2>/dev/null || true
            sleep 3
          done
          echo "::error::Could not push star history after retries."; exit 1
