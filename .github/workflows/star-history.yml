# Self-hosted star history chart, rendered with star-history's OWN renderer.
# star-history.com's public /svg API now fails (GitHub restricted stargazer data
# and their server token pool is rate-limited), but their chart code is MIT and
# accepts a caller token. So we clone it, run their renderer (shared/packages/
# xy-chart) with our repo token via a small driver (assets/star-history/render.mts),
# and commit the resulting self-contained SVG. Refreshed weekly, committed only
# when it changes. No third-party request at read time; the README never breaks.
name: Star history chart

on:
  schedule:
    - cron: "17 4 * * 1" # Mondays 04:17 UTC
  workflow_dispatch:

permissions:
  contents: write

jobs:
  chart:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - name: Check out star-history (its renderer is MIT)
        uses: actions/checkout@v4
        with:
          repository: star-history/star-history
          path: sh
      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Render chart with star-history's renderer (our token)
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: euniai/awesome-code-agents
        run: |
          set -euxo pipefail
          cp assets/star-history/render.mts sh/backend/render.mts
          # npm (not pnpm): pnpm 10+ blocks dependency build scripts, which tsx's
          # esbuild needs; npm runs them normally.
          ( cd sh && npm install --no-audit --no-fund )
          ( cd sh/backend && npm install --no-audit --no-fund )
          ( cd sh/backend && OUT="$GITHUB_WORKSPACE/assets/star-history.svg" npx tsx render.mts )
          rm -rf sh

      - name: Commit if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets/star-history.svg
          if git diff --cached --quiet; then
            echo "Star history unchanged."
            exit 0
          fi
          git commit -m "chore(assets): refresh star history chart"
          for attempt in 1 2 3; do
            git fetch --quiet origin main
            if git rebase --quiet origin/main && git push --quiet; then
              echo "Pushed on attempt $attempt."; exit 0
            fi
            git rebase --abort 2>/dev/null || true
            sleep 3
          done
          echo "::error::Could not push star history after retries."; exit 1
