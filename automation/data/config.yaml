repo:
  owner: EuniAI
  name: awesome-code-agents
  reviewer: Zhaoyang-Chu
inbox:
  issue_number: 4
review:
  label: paper-review
  # Backpressure: when this many papers are already awaiting review in open
  # issues, historical intakes (weekend backfill, backlog) pause until the pool
  # drains. The daily crawl is never gated: fresh papers keep flowing.
  pool_cap: 50
# Weekend historical sweep: arXiv announces no new papers on weekends, so the
# Saturday/Sunday crawl runs advance a backfill cursor through history instead,
# by ANNOUNCEMENT date (same OAI source and same ledger as the daily crawl).
# Everything is re-swept with the NEW keyword net (the old one missed whole
# branches). Cursor state lives in data/backfill.json; delete the section to stop.
backfill:
  start: "2025-06-01" # owner's chosen sweep horizon
  until: "2026-07-11" # the relaunch date; daily harvesting covers everything after
  slice_days: 4 # history processed per weekend run (when the pool has room)
arxiv:
  # The daily crawl is announcement-driven (OAI-PMH): each run ingests exactly
  # the mailings since the last successful run, tracked by data/harvest.json.
  # No lookback window exists; a missed day self-heals on the next run.
  # Category recall net, mapped to the taxonomy branches:
  # software (cs.SE, cs.PL, cs.CR), agents/models (cs.AI, cs.CL, cs.LG),
  # worlds and domains (cs.RO robots, cs.AR hardware, cs.OS+cs.DC systems, cs.DB).
  categories:
    - cs.SE
    - cs.AI
    - cs.CL
    - cs.LG
    - cs.PL
    - cs.CR
    - cs.RO
    - cs.AR
    - cs.OS
    - cs.DC
    - cs.DB
  # Cheap whole-phrase pre-filter on title+abstract (case-insensitive, word
  # boundaries). Recall net only: the classifier still judges relevance.
  keywords:
    # agents for code
    - code agent
    - coding agent
    - software agent
    - SWE agent
    - agentic coding
    - agentic software
    - vibe coding
    - code LLM
    - coding LLM
    # generation and completion
    - code generation
    - code completion
    - program synthesis
    - code synthesis
    - repository-level
    - repo-level
    # debugging and issue resolution
    - SWE-bench
    - issue resolution
    - issue resolving
    - issue reproduction
    - program repair
    - automated repair
    - bug fix
    - bug localization
    - fault localization
    # testing, review, security
    - test generation
    - unit test
    - fuzzing
    - code review
    - pull request
    - vulnerability detection
    - vulnerability repair
    - vulnerability localization
    - secure code
    # maintenance and comprehension
    - code translation
    - code migration
    - refactoring
    - code optimization
    - code performance
    - code summarization
    - code comprehension
    - code understanding
    - code search
    - code documentation
    - environment setup
    - build repair
    - build failure
    # domains: web, db, systems, hardware, cad, graphics, games
    - screenshot-to-code
    - design-to-code
    - UI code
    - text-to-SQL
    - text2sql
    - NL2SQL
    - SQL generation
    - GPU kernel
    - CUDA kernel
    - kernel generation
    - Verilog
    - VHDL
    - RTL generation
    - hardware description
    - chip design
    - text-to-CAD
    - CAD program
    - parametric CAD
    - SVG generation
    - visualization code
    - chart generation
    # code as action (agency worlds)
    - code-as-policy
    - code as policies
    - code as action
    - code-as-action
    - executable code action
    - CodeAct
    - terminal agent
    - CLI agent
    - terminal
    - CLI
    - command-line
    - shell
    - command-line agent
    - shell agent
    - computer-use agent
    - web agent
    - browser agent
    - Minecraft
    - OS
    - Operating Systems
    # research and discovery
    - machine learning engineering
    - MLE-bench
    - AutoML agent
    - data science agent
    - data analysis agent
    - AI scientist
    - theorem proving
    - proof assistant
    - formal verification
