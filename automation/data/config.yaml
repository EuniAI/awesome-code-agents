repo:
  owner: EuniAI
  name: awesome-code-agents
  reviewer: Zhaoyang-Chu
inbox:
  issue_number: 4
review:
  label: paper-review
  # Backpressure: when this many papers are already awaiting review in open
  # issues, historical intakes (weekend backfill, backlog) pause until the pool
  # drains. The daily crawl is never gated: fresh papers keep flowing.
  pool_cap: 50
# Weekend historical sweep: arXiv announces no new papers on weekends, so the
# Saturday/Sunday crawl runs advance a backfill cursor through history instead,
# by ANNOUNCEMENT date (same OAI source and same ledger as the daily crawl).
# Everything is re-swept with the NEW keyword net (the old one missed whole
# branches). Cursor state lives in data/backfill.json; delete the section to stop.
backfill:
  start: "2025-06-01" # owner's chosen sweep horizon
  until: "2026-07-11" # the relaunch date; daily harvesting covers everything after
  slice_days: 4 # history processed per weekend run (when the pool has room)
arxiv:
  # The daily crawl is announcement-driven (OAI-PMH): each run ingests exactly
  # the mailings since the last successful run, tracked by data/harvest.json.
  # No lookback window exists; a missed day self-heals on the next run.
  # Category recall net, mapped to the taxonomy branches:
  # software (cs.SE, cs.PL, cs.CR), agents/models (cs.AI, cs.CL, cs.LG),
  # worlds and domains (cs.RO robots, cs.AR hardware, cs.OS+cs.DC systems, cs.DB).
  categories:
    - cs.SE
    - cs.AI
    - cs.CL
    - cs.LG
    - cs.PL
    - cs.CR
    - cs.RO
    - cs.AR
    - cs.OS
    - cs.DC
    - cs.DB
  # Recall net: a cheap title+abstract pre-filter (case-insensitive, whole-word,
  # plural-tolerant). A paper passes if it contains ANY keyword below.
  #
  # DESIGN STANCE: recall over precision. A false positive costs one classifier
  # call and is then auto-skipped (pipeline.classify_and_propose drops out-of-scope
  # before the review pool), so noise never reaches the owner. A false negative is
  # INVISIBLE and permanent: a paper the net misses never enters the pipeline, and
  # no later stage can recover it. So the net leans broad and delegates precision
  # to the classifier. When unsure whether a term is too noisy, keep it.
  #
  # Broad, polysemous tokens (terminal, shell, game, database) are intentional:
  # they over-recall (e.g. an RL "terminal state") and the classifier filters them.
  # The one deliberate line: bare agent/model words (agent, LLM, language model)
  # are NOT keywords, because alone they match most of cs.AI/CL/LG and would turn
  # the filter off rather than widen it. Code-specific agent/model phrases ARE in.
  # Grouped by leaf so coverage is auditable; grow it freely.
  keywords:
    # scope indicators: agents & models for code
    - code agent
    - coding agent
    - software agent
    - software engineering agent
    - SWE agent
    - SWE-agent
    - code model
    - coding model
    - code LLM
    - coding LLM
    - code language model
    - copilot
    - coding assistant
    - code assistant
    - programming assistant
    - foundation model
    - frontier model
    - agentic coding
    - agentic software engineering
    - vibe coding
    # generation & completion
    - code generation
    - code completion
    - program synthesis
    - code synthesis
    - repository-level
    - repo-level
    # debugging & issue resolution
    - SWE-bench
    - issue resolution
    - issue resolving
    - issue reproduction
    - program repair
    - automated program repair
    - bug fixing
    - bug localization
    - fault localization
    # testing, review, security
    - test generation
    - test-case generation
    - unit test
    - fuzzing
    - code review
    - pull request
    - vulnerability detection
    - vulnerability repair
    - vulnerability localization
    - secure code
    - penetration testing
    # maintenance & comprehension
    - code translation
    - code migration
    - refactoring
    - code optimization
    - code performance
    - code summarization
    - code comprehension
    - code understanding
    - code search
    - code documentation
    # infrastructure
    - environment setup
    - build repair
    - build failure
    - continuous integration
    - CI/CD
    - devops
    # web
    - screenshot-to-code
    - design-to-code
    - UI code
    - web development
    - frontend
    - web application
    - web page
    # database
    - text-to-SQL
    - text2sql
    - NL2SQL
    - SQL generation
    - SQL query
    - database
    - query optimization
    # systems
    - GPU kernel
    - CUDA kernel
    - Triton kernel
    - kernel generation
    - compiler
    - systems programming
    - high-performance computing
    - parallel programming
    # hardware
    - Verilog
    - VHDL
    - RTL
    - hardware description
    - hardware design
    - chip design
    - circuit design
    - FPGA
    # cad
    - text-to-CAD
    - CAD
    - parametric CAD
    - computer-aided design
    - 3D modeling
    - mesh generation
    # graphics
    - SVG generation
    - visualization code
    - chart generation
    - data visualization
    - computer graphics
    - animation
    - rendering
    - shader
    # game (artifact + world)
    - game development
    - game design
    - game engine
    - level generation
    - procedural content
    - playable game
    - video game
    - game playing
    - Minecraft
    # code as action (agency worlds)
    - code-as-policy
    - code as policies
    - code as action
    - code-as-action
    - executable code action
    - CodeAct
    - reward function generation
    - embodied agent
    # terminal / OS world (broad on purpose)
    - terminal
    - terminal environment
    - terminal emulator
    - shell
    - shell command
    - shell script
    - bash
    - command line
    - command-line
    - CLI
    - operating system
    - sysadmin
    - system administration
    # browser & apps worlds
    - web browser
    - web navigation
    - web browsing
    - browser
    - browser automation
    - website
    - GUI
    - graphical user interface
    - desktop application
    - mobile application
    - mobile app
    - computer use
    - computer-use
    - spreadsheet
    - office software
    # research & discovery world
    - machine learning engineering
    - MLE-bench
    - AutoML
    - automated machine learning
    - data science
    - data analysis
    - scientific discovery
    - research automation
    - hypothesis generation
    - AI scientist
    - jupyter notebook
    - kaggle
    - theorem proving
    - proof assistant
    - formal verification
    # studies (about the agents/field)
    - empirical study
    - user study
    - case study
    - controlled experiment
    - literature review
    - systematic literature review
    - survey
    - developer productivity
    - software developers
    - practitioners
    - developer experience
    - human-AI collaboration
    - generative AI
    - AI-assisted
    - ChatGPT
    # foundation models / software engineering at large
    - software engineering
    - software development
    - coding benchmark
    - code intelligence
    - static analysis
